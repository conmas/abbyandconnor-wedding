extends layout

block left
	+photo('background-image:url(images/bucktown.jpeg)','Chicago, the night we got engaged')

block right
	+breadcrumbs('/','Return home')
	h1=error.status
	h2=message
	h3 Hmm, something went wrong. You should probably just <a href="/">return home</a>.
	p(style='font-size: 0.5em') #{error.stack}